# 사람을 위한 협업 안내서

> 이 문서는 두 팀원이 먼저 읽고 합의해야 하는 운영 규칙입니다. AI 작업 지침은 `AGENTS.md`에 따로 있습니다.

## 1. 우리가 만드는 것

SwiftUI, RealityKit, ARKit을 이용해 Apple Vision Pro에서 양손 Hand Pose를 인식하고 공간 술법 효과를 만드는 기초 튜토리얼을 제작합니다. 결과물은 실행 가능한 visionOS 예제 프로젝트와 단계별 DocC 튜토리얼이며, GitHub Pages에 정적으로 배포합니다.

이 프로젝트의 우선순위는 다음과 같습니다.

1. 학습자가 그대로 따라 했을 때 재현할 수 있는가
2. 코드와 API 설명이 현재 지원 환경에서 정확한가
3. 실기기에서 확인한 것과 추정한 것을 구분했는가
4. 화려함보다 손 추적 원리와 디버깅 방법을 이해할 수 있는가
5. 두 사람과 각자의 AI가 같은 결정을 읽을 수 있는가

## 2. 먼저 알아야 할 저장소 문서

| 문서 | 독자 | 역할 |
| --- | --- | --- |
| `TEAM_COLLABORATION_GUIDE.md` | 사람 | 협업 순서와 Git 컨벤션의 기준 |
| `AGENTS.md` | AI와 사람 | AI가 작업할 때 지켜야 할 범위와 검증 규칙 |
| `docs/PROJECT_FOUNDATION.md` | 사람과 AI | 합의된 프로젝트 원칙, 학습 여정, 기술 기준과 미결정 사항 |
| `README.md` | 모든 방문자 | 프로젝트 소개와 시작점 |
| GitHub Issue | 담당자, 리뷰어, AI | 아직 끝나지 않은 작업의 명세와 토론 |
| Pull Request | 담당자와 리뷰어 | 실제 변경, 검증 결과와 병합 판단 |

정보가 충돌하면 다음 순서로 신뢰합니다.

1. 기본 브랜치에 병합된 코드와 DocC
2. 기본 브랜치에 병합된 협업·결정 문서
3. 열린 Issue와 PR에서 합의된 최신 내용
4. AI 대화와 개인 메모

AI 답변은 유용한 제안이지만 사실의 근거도, 팀의 결정도 아닙니다. API 사실은 공식 문서나 실제 빌드로 확인하고, 방향은 Issue와 PR을 통해 두 사람이 합의합니다.

## 3. 역할과 책임

방향이 정해지지 않은 탐색 단계에서는 **Explorer**와 **Challenger**, 구현 단계에서는 **작성자**와 **리뷰어**를 정합니다.

### Explorer와 Challenger

- Explorer는 공식 자료와 가능한 방향을 넓게 조사하고 여러 선택지를 제안한다.
- Challenger는 제안을 바로 개선하지 않고 반례, 누락, 검증되지 않은 가정과 과도한 범위를 찾는다.
- 두 사람은 각자의 AI로 먼저 독립적으로 탐색한 뒤 결과를 Issue 댓글에서 비교한다.
- 두 AI가 같은 답을 했다는 이유만으로 채택하지 않고, 공식 근거나 작은 Spike로 확인한다.

### 작성자

- Issue의 목표와 범위를 명확하게 만든다.
- 자신의 브랜치에서 코드와 문서를 함께 수정한다.
- AI가 만든 내용을 이해하고 검토한 뒤 커밋한다.
- 수행한 검증과 수행하지 못한 검증을 PR에 기록한다.

### 리뷰어

- Issue의 완료 조건을 기준으로 결과를 다시 확인한다.
- 설명만 읽지 않고 가능한 범위에서 빌드하거나 재현한다.
- API 정확성, 학습 흐름, 코드와 DocC의 일치를 확인한다.
- 확신이 낮은 부분에는 승인 대신 질문이나 변경 요청을 남긴다.

작성자와 리뷰어는 Issue마다 교대합니다. 본인이 작성한 PR을 혼자 승인하고 병합하지 않습니다.

## 4. 표준 작업 흐름

### 4.0 아무것도 정해지지 않은 경우

구현 Issue를 바로 만들지 않고 통합 템플릿으로 `[DISCOVERY] 프로젝트 Genesis`를 엽니다.

```text
Genesis Issue
→ 독립적인 AI 탐색
→ Explorer와 Challenger의 상호 검토
→ 선택지를 [DECISION] Issue에서 합의
→ docs/PROJECT_FOUNDATION.md 반영 PR
→ 불확실한 기술을 [SPIKE]로 검증
→ 장 단위 [TUTORIAL] Issue
```

초기 탐색에서 기술 스택과 파일 구조부터 고정하지 않습니다. 먼저 대상 독자, 학습 완료 결과, 반드시 포함할 개념, 제외할 범위와 성공 증거를 합의합니다. 합의된 내용만 `docs/PROJECT_FOUNDATION.md`에 옮기며, Issue와 AI 대화에만 있는 내용은 프로젝트의 확정 기준으로 취급하지 않습니다.

### 4.1 Issue를 먼저 만든다

오탈자 한두 개를 제외한 모든 작업은 Issue에서 시작합니다.

모든 Issue는 `.github/ISSUE_TEMPLATE/project-work.md` 통합 양식을 사용하고 제목에 유형을 표시합니다.

| 태그 | 사용 시점 |
| --- | --- |
| `[DISCOVERY]` | 대상 독자, 문제, 학습 흐름과 가능한 방향 탐색 |
| `[DECISION]` | 둘 이상의 선택지를 비교하고 팀 기준 확정 |
| `[SPIKE]` | 불확실한 API, 기기 동작 또는 배포의 최소 기술 실험 |
| `[TUTORIAL]` | 앱 코드, DocC 장과 공간 효과 제작 |
| `[VERIFY]` | 컴파일, 문서, Simulator, 실기기와 배포 확인 |
| `[FIX]` | 재현 가능한 오류 또는 코드·문서 불일치 수정 |

Issue는 AI에게 그대로 전달할 작업 명세입니다. 목표, 하지 않을 일, 공식 근거, 완료 조건이 없으면 구현을 시작하지 않습니다.

### 4.2 담당자와 리뷰어를 정한다

한 Issue를 두 사람이 동시에 구현하지 않습니다. 병렬 작업이 필요하면 수정 파일이 겹치지 않도록 Issue를 나눕니다.

### 4.3 Issue용 브랜치를 만든다

브랜치 이름은 영문 소문자와 하이픈을 사용합니다.

```text
discovery/1-project-genesis
spike/7-hand-tracking
docs/12-visualize-hand-joints
feat/18-hand-pose-classifier
fix/24-docc-base-path
ci/31-deploy-github-pages
```

허용하는 접두사는 다음과 같습니다.

| 접두사 | 용도 |
| --- | --- |
| `discovery/` | 프로젝트 탐색과 기초 문서 |
| `spike/` | 버려도 되는 최소 기술 검증 |
| `feat/` | 앱 또는 튜토리얼의 새 기능 |
| `docs/` | DocC와 설명 중심 변경 |
| `fix/` | 오류 수정 |
| `test/` | 테스트와 검증 자료 |
| `ci/` | GitHub Actions와 배포 |
| `chore/` | 설정과 유지보수 |

### 4.4 AI에게 공통 맥락을 준다

AI에게 작업을 요청할 때 최소한 다음과 같이 지시합니다.

```text
저장소의 AGENTS.md, docs/PROJECT_FOUNDATION.md와 Issue #12를 먼저 읽어.
Issue 범위 밖의 변경은 하지 말고, 기존 변경을 보존해.
사용한 Apple API는 공식 문서와 대조해.
실기기에서 검증하지 못한 내용은 명확히 표시해.
완료 후 변경 파일과 수행한 검증을 요약해.
```

AI에게 팀원의 긴 대화를 그대로 전달하기보다, 필요한 결론을 Issue에 정리합니다. 반대로 AI가 중요한 선택지를 제시하면 Issue 댓글에 요약해 상대방이 볼 수 있게 합니다.

### 4.5 작게 커밋한다

한 커밋에는 설명 가능한 하나의 의도를 담습니다. 빌드 가능한 중간 지점을 우선합니다.

커밋 형식은 다음을 사용합니다.

```text
<type>: <짧은 요약>
```

권장 type은 다음과 같습니다.

```text
feat: 양손 관절 디버그 표시 추가
docs: 월드 좌표 변환 설명 보강
fix: 추적 해제 시 효과가 남는 문제 수정
test: 핀치 임계값 실기기 결과 기록
ci: DocC Pages 배포 워크플로 추가
chore: Issue와 PR 템플릿 추가
refactor: 포즈 판정과 효과 제어 분리
```

커밋 제목은 한글을 사용해도 됩니다. `수정`, `업데이트`처럼 무엇이 달라졌는지 알 수 없는 제목은 피합니다.

### 4.6 Draft PR을 일찍 연다

작업 방향을 공유할 필요가 있으면 완성 전 Draft PR을 엽니다. PR 본문의 `Closes #번호`로 Issue를 연결합니다.

PR에는 AI 대화 전체가 아니라 다음을 기록합니다.

- 결과적으로 무엇이 달라졌는가
- AI가 어떤 역할을 했는가
- 사람이 무엇을 직접 확인했는가
- 어떤 환경에서 검증했는가
- 무엇을 아직 확인하지 못했는가
- 리뷰어가 특히 판단해야 할 부분은 무엇인가

### 4.7 상대방이 리뷰하고 병합한다

리뷰어는 최소한 다음을 확인합니다.

- Issue 범위를 벗어나지 않았는가
- 완료 조건을 충족했는가
- 새 API와 기술적 주장이 공식 자료에 근거하는가
- 앱 코드와 DocC 코드 리스팅이 일치하는가
- Simulator와 실기기 검증이 구분되어 있는가
- 초급 학습자가 중간 단계를 건너뛰지 않고 따라갈 수 있는가

변경 요청이 해결되고 필수 검증이 끝난 뒤 기본 브랜치에 병합합니다.

## 5. Issue와 PR 크기

원칙적으로 하나의 PR은 하나의 Issue를 해결합니다. 다음 항목이 동시에 들어가면 Issue 분리를 검토합니다.

- 서로 독립적인 두 개 이상의 술법
- 앱 구조 변경과 배포 구조 변경
- 대규모 리팩터링과 신규 튜토리얼 작성
- 실기기 검증 없이 여러 장의 코드를 한꺼번에 작성하는 작업

좋은 Issue는 반나절에서 이틀 안에 리뷰 가능한 결과가 나오는 크기입니다. 너무 큰 Issue는 `준비`, `손 추적`, `포즈 판정`, `효과`, `배포`로 나눕니다.

## 6. 파일과 문서 동기화 규칙

DocC의 코드 리스팅을 앱 코드와 별도로 복사해 관리할 경우 반드시 함께 갱신합니다.

```text
앱 코드 변경
→ 관련 DocC 설명 확인
→ Resources/CodeListings 확인
→ Product > Build Documentation
```

코드와 문서가 의도적으로 다른 단계의 상태를 보여준다면, 파일 이름과 본문에서 그 차이를 설명합니다.

새 이미지는 의미 있는 영문 파일 이름을 사용합니다.

```text
hand-joints-debug-view.png
two-hand-pinch-lightning.png
docc-pages-deployment.png
```

`final.png`, `new-final-2.png` 같은 이름은 사용하지 않습니다.

## 7. API, 버전과 출처 규칙

- ARKit, RealityKit, SwiftUI, DocC 설명은 Apple Developer Documentation과 공식 샘플을 우선합니다.
- GitHub Actions와 Pages 설명은 GitHub 공식 문서를 우선합니다.
- 블로그와 AI 답변은 탐색용으로 사용할 수 있지만 최종 근거로 단독 사용하지 않습니다.
- Xcode, visionOS, GitHub Action 버전은 바뀔 수 있으므로 확인 날짜와 검증 환경을 남깁니다.
- 임계값은 공식값처럼 표현하지 않고 예제의 시작값임을 밝힙니다.
- 컴파일하지 않은 코드를 “동작한다”고 표현하지 않습니다.
- Apple Vision Pro에서 확인하지 않았다면 “실기기 검증 필요”라고 씁니다.

## 8. AI 활용 컨벤션

AI는 다음 작업에 적극적으로 사용합니다.

- 공식 자료 후보 탐색과 비교
- 튜토리얼 목차와 학습 흐름 제안
- 반복 코드의 초안 작성
- 테스트 체크리스트와 실패 가능성 도출
- PR의 변경 사항 검토
- 두 구현안의 장단점 비교

AI에게 맡긴 뒤 사람이 반드시 확인할 항목은 다음과 같습니다.

- API 이름과 지원 플랫폼
- 좌표계와 행렬 곱 순서
- Swift 동시성 및 생명주기
- 권한과 Info.plist 설정
- DocC 지시문과 리소스 경로
- GitHub Pages 하위 경로와 CI 버전
- 손 추적 임계값의 실기기 적합성

AI가 서로 다른 답을 내면 다수결로 고르지 않습니다. 차이를 `[DECISION]` Issue로 옮기고 공식 자료, 작은 프로토타입 또는 실기기 결과로 판단합니다.

## 9. 완료의 정의

### 코드가 포함된 작업

- 현재 합의한 Xcode와 visionOS SDK에서 빌드된다.
- 미지원 환경과 권한 거부를 처리한다.
- 추적이 끊겼을 때 잘못된 효과가 남지 않는다.
- 임계값과 시간 조건의 의미가 설명되어 있다.
- 관련 DocC와 코드 리스팅이 갱신되어 있다.

### DocC 작업

- `Product > Build Documentation`이 성공한다.
- 학습 목표, 단계, 결과 확인과 오류 해결 방법이 있다.
- 내부 링크, 이미지, 코드 리스팅이 열린다.
- 공식 자료 링크와 실기기 필요 여부가 명시되어 있다.

### 배포 작업

- GitHub Actions 로그가 성공한다.
- 저장소 하위 경로에서 CSS, 이미지와 내부 링크가 열린다.
- 새로고침과 직접 URL 접근을 확인한다.
- 배포 주소와 수동 재배포 방법이 README에 반영되어 있다.

## 10. 첫 협업 순서

1. 두 사람이 이 문서, `AGENTS.md`, `docs/PROJECT_FOUNDATION.md`를 읽는다.
2. `[DISCOVERY] 프로젝트 Genesis` Issue를 열고 각자 AI의 독립적인 제안을 댓글로 남긴다.
3. 한 명은 Explorer, 다른 한 명은 Challenger가 되어 대상 독자, 완료 결과, 범위와 위험을 비교한다.
4. 합의가 필요한 항목을 `[DECISION]` Issue로 분리한다.
5. 합의 결과를 `docs/PROJECT_FOUNDATION.md`에 반영하는 첫 PR을 만든다.
6. Hand Tracking, Pose Classification, DocC Pages를 각각 작은 `[SPIKE]`로 확인한다.
7. Spike 결과를 기초 문서에 반영하고 튜토리얼 목차를 확정한다.
8. `[TUTORIAL]` Issue를 장 단위로 만들고 작성자와 리뷰어 역할을 교대한다.
9. Apple Vision Pro 테스트가 필요한 Issue를 명확히 표시하고 구현 직후 검증한다.

작업 속도보다 “어떤 근거로 무엇을 확인했는지 다음 사람이 재현할 수 있는 상태”를 우선합니다.
